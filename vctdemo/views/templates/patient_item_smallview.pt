<div class="smallview item">
 
<a href="${patient_url}/${context.id}/edit" class="ui-button ui-state-default ui-corner-all action">Edit</a>

<a href="${context.id}/">
<span class="date">${context.date}</span> : Probl :
<span class="title">${context.title}</span>, 
by <span class="author">${context.author}</span>
<span> (Ref ${context.id}, v=${context.version})</span>
<tal:block define="max 200">
<br/><span tal:content="python:context.text[:max]"> : </span>
<a tal:condition="python:len(context.text)>max" href="${context.id}/"> (...read more...) </a>
</tal:block>
</a>
</div>
