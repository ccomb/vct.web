<div class="smallview item">
 
<span></span>

<a href="${context.id}/">
<span>( ${context.id} ) ITEM: ${context.title}</span>
      </a>

<tal:block define="max 200">
<span tal:content="python:context.text[:max]"> : 
</span>
<a tal:condition="python:len(context.text)>max" href="${context.id}/"> (...read more...) </a>
</tal:block>

</div>
