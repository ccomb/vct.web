<div class="smallview item">
 
<span></span>

<a href="${context.id}/">
<span>( ${context.id} ) ITEM: ${context.title}</span>
      </a>

<tal:block define="max 300">
<span tal:content="python:context.text[:max]"> : </span>
<a tal:condition="python:len(context.text)>max" href="${context.id}/"> (...read more...) </a>
/ (by <span tal:content="python:context.author"></span>), 
V=0, 
<a href="${patient_url}/${context.id}/edit" class="ui-state-default ui-corner-all">Edit</a>, 
<a href="${patient_url}add" class="ui-state-default ui-corner-all">Add</a>
</tal:block>

</div>
